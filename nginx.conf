server {
    listen 8080;
    root /usr/share/nginx/html;

    # ① 直接存在するファイルはそのまま返す（index.html、login.html など）
    location / {
        try_files $uri $uri/ =404;
    }

    # ② アプリ内ルーティング（例: /view/123）のみ index.html にフォールバック
    location /app/ {
        try_files $uri /index.html;
    }
}
